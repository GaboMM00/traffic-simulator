/**
 * Capa de etiquetas del mapa. Actualmente no muestra nada en el canvas.
 * Las etiquetas de intersección solo aparecen en tooltips al hacer hover.
 * Se mantiene como placeholder para el sistema de tooltips del canvas.
 */

export default function StreetLabels() {
  return null
}
